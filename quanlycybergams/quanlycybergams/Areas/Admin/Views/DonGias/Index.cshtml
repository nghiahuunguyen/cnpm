@model IEnumerable<quanlycybergams.Models.DonGia>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GiaMay)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GiaMay)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID_gia }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID_gia }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID_gia })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
